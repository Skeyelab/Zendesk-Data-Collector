## Learned User Preferences

- Prefer TDD; unit testing is important.
- Prefer yarn over npm.
- Tests and builds must pass before opening or merging PRs.
- Never commit to main; suggest or create a new branch when needed.
- Keep code concise but smart.
- For Ruby work, run `bin/standardrb --fix` before committing.
- Responses need not include positive affirmations.

## Learned Workspace Facts

- Use rvm to use the project’s Ruby version and gemset (see `.ruby-version`).
- Rails 8 app: Postgres, Solid Queue, Avo admin, Zendesk API; Ruby 3.2.4.
- Lint Ruby with `bin/standardrb --fix`.
